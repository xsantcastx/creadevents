# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Write .env for build
        run: |
          cat <<'EOF' > .env
          FIREBASE_API_KEY=${{ secrets.FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN=creadevents-b29b9.firebaseapp.com
          FIREBASE_PROJECT_ID=creadevents-b29b9
          FIREBASE_STORAGE_BUCKET=creadevents-b29b9.firebasestorage.app
          FIREBASE_MESSAGING_SENDER_ID=502006799979
          FIREBASE_APP_ID=1:502006799979:web:1cf98e9a3179c724fb9b2e
          FIREBASE_MEASUREMENT_ID=G-E9LXFPZYNZ
          RECAPTCHA_SITE_KEY=6LcYpA8sAAAAAC81J5b5a5WFyLsK6vhPWFBwibQK
          APPCHECK_PROVIDER=recaptcha
          APPCHECK_SITE_KEY=6LcYpA8sAAAAAC81J5b5a5WFyLsK6vhPWFBwibQK
          GA_MEASUREMENT_ID=G-E9LXFPZYNZ
          GTM_CONTAINER_ID=GTM-XXXXXXX
          APP_NAME=Creation Design & Events
          APP_URL=https://creadevents.com
          APP_BRAND_KEY=default
          USE_FIREBASE_EMULATORS=false
          EOF
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_CREADEVENTS_B29B9 }}
          projectId: creadevents-b29b9
